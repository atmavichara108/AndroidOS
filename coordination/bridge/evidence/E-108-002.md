---
id: E-108-002
task_id: AOS-T108-001
producer: vault-librarian
kind: protocol-report
observed_at: "2026-08-29"
source_repo: AndroidOS
status: partial
---

## Report disposition

Зафиксирован новый protocol report T-108 без переноса его текста в bridge.
Named runtime session: `ses_fb0ee381fffeHfjxggBF0CXpm3/`.

- fallback не использовался;
- permissions наблюдались как статические правила;
- host mutation и изменения dotfiles WIP отсутствуют;
- E-108-002 не создан самим `system-ops`;
- verdict: `BLOCKED`.

Runtime blockers из отчёта:

1. edit canonical evidence path был denied;
2. read task/handoff через `external_directory` был denied.

## Read-only permission finding

Проверены schema/docs и merged config. Schema допускает object rules для
`edit` и `external_directory`; docs указывают, что последний применяется к
любому tool, работающему с путём вне workspace, и что последнее совпавшее
правило побеждает. В merged config для named `system-ops` присутствуют:

```yaml
edit:
  "*": deny
  "/home/rudra/Projects/AndroidOS/coordination/bridge/evidence/**": allow
external_directory:
  "*": deny
  "/home/rudra/Projects/AndroidOS/coordination/bridge/evidence/**": allow
```

Следствие: task/handoff вне разрешённого evidence subtree блокируются
намеренно. Статический merge не объясняет edit denial для evidence; наиболее
вероятная runtime причина — уже запущенная сессия не перечитала добавленный
uncommitted agent config. Это runtime hypothesis, не доказанная причина.

## Commands and results

- `opencode debug config` в Vault: exit 0; merged `system-ops` содержит named
  agent и оба scoped правила.
- `read https://opencode.ai/config.json`: exit 0; schema содержит
  `PermissionConfig`, `edit` и `external_directory`.
- `read https://opencode.ai/docs/permissions/`: exit 0; docs подтверждают
  external-directory boundary и last-match semantics.
- `readlink -f /home/rudra/.config/opencode/agent/system-ops.md`: exit 0;
  resolved source is dotfiles uncommitted WIP.
- `test ! -e .../evidence/E-108-002.md`: exit 0 до создания artifact.
- SHA/path validation: exit 0 для Vault `576a2e5936ebfa8fc7126fc035df90d784779ee8`
  (`TASKS.md`, `03-Projects/dotfiles.md`, canonical spec), dotfiles
  `1b7d45cf5c9968b3726662123684057fcf9730f3` (`.opencode/agent/sysop.md`) и
  AndroidOS `c8c8ca896b77bf8374a915d4d0b40d99b201f054` (`AGENTS.md`).

## Provenance and gaps

Этот report uncommitted и не имеет `source_commit`; baseline commit не
используется как provenance bridge. Приведённые refs сохранены как проверенные
baseline/unverified context, а не как происхождение этого evidence. Изменения
в host, dotfiles WIP, Stow, commit и push не выполнялись. Не доказаны:
fresh-session behavior, успешная runtime запись evidence и полная причина
edit denial.

## Safe fix proposal

Не расширять allowlist. Сначала завершить текущую сессию и запустить новую после
проверки того же merged config. Для чтения task/handoff named role нужен
отдельный минимальный read-only scoped `external_directory` allow только на
`/home/rudra/Projects/AndroidOS/coordination/bridge/tasks/**` и
`/home/rudra/Projects/AndroidOS/coordination/bridge/handoffs/**`; edit оставлять
только на `evidence/**`. Затем выполнить
один controlled runtime smoke с проверкой отсутствия mutation. До этого T-108
остаётся `BLOCKED`.
