---
id: E-109-002
task_id: AOS-BRIDGE-001
producer: builder
kind: structural-example
observed_at: "2026-08-28T14:10:00Z"
source_repo: AndroidOS
status: partial
example: non-observed-fixture
---

# Safe conflict/stale/append-only example

This is a local fixture specification, not a claim that a conflict or stale
task occurred. It is safe to verify in a temporary directory and never writes
the repository, host configuration, device state, or live data.

```sh
tmp=$(mktemp -d)
trap 'rm -rf "$tmp"' EXIT
printf '%s\n' 'owner: planner' 'status: in_progress' > "$tmp/task.md"
cp "$tmp/task.md" "$tmp/task-before.md"
printf '%s\n' 'owner: builder' >> "$tmp/task.md"
test "$(grep -c '^owner:' "$tmp/task.md")" -eq 2
cmp -s "$tmp/task.md" "$tmp/task-before.md"; test $? -eq 1
printf '%s\n' 'conflict: blocked; no silent merge; prior file preserved separately'
printf '%s\n' 'stale: in_progress with no update > 7 days -> stale'
printf '%s\n' 'append-only: correction is a new evidence/decision artifact'
```

Expected output is the three labelled lines above and exit code 0. The fixture
demonstrates the rule on one explicit example; it does not create a verdict.
